<!DOCTYPE html>
<html>
<head>
  <title>Wellness Tracker</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<header>
  <h1 data-role="title">Wellness Tracker</h1>
  <% if user_signed_in? %>
    <span>
      Signed in as <%= current_user.email %>. <%= link_to "Sign Out", logout_path, method: :delete %>
    </span>
  <% else %>
    <span><%= link_to "Log In", login_path %></span>
    <span><%= link_to "Sign Up", new_user_path, data: { role: "signup" } %></span>
  <% end %>
</header>

<%= yield %>

</body>
</html>
